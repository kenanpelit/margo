/.cache
/.vscode
.codex
.claude
.agents
/result
CLAUDE.md
.mcp.json
# Rust build artifacts
/target/

# mkdocs build output (W4.3)
/site/

# Throwaway screenshots that accumulate in repo root during
# UI work (dashbord*.png from the dashboard work, test.png
# from screenshot-widget iterations, …). Add specific names
# to .git/info/exclude if you want to keep one.
*.png
# …but the bundled default desktop wallpaper is a real asset.
!assets/wallpapers/margo-hero.png

