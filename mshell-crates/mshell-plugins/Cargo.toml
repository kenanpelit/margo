[package]
name = "mshell-plugins"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "MShell plugin manager — install declarative widgets from external git repositories."
repository.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
toml.workspace = true
dirs.workspace = true
# Backend (async-secret-service) is pinned once in the workspace dep; don't
# re-add sync-secret-service here or keyring refuses to build (sync + async).
keyring = { workspace = true }
thiserror.workspace = true
tracing.workspace = true

[lints]
workspace = true
