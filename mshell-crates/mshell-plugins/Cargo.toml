[package]
name = "mshell-plugins"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "MShell plugin manager — install declarative widgets from external git repositories."
repository.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
toml.workspace = true
dirs.workspace = true
# Backend + store selection (zbus, via keyring 4's `v1` feature) is pinned
# once in the workspace dep; don't add features here.
keyring = { workspace = true }
thiserror.workspace = true
tracing.workspace = true

[lints]
workspace = true
