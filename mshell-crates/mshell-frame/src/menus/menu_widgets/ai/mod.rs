//! Native AI assistant menu — a streaming multi-provider chat panel, the core
//! port of the `assistant-panel` WASM plugin. Config + transport live in the
//! `mshell-ai` engine; this is just the in-shell layer-shell chat surface.

pub(crate) mod ai_menu_widget;
